# product_info_extract
This project extracts product details such as name, price, and rating from a web page using HTML, CSS, and JavaScript. The extracted data is displayed in a table and can be exported as a CSV file.
